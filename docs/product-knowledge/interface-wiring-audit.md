# 音曼接口接线专项审计

更新时间：2026-07-18

本文件只保存接口接线校准所需的精简证据、用户最终口径和待补字段。优先级固定为：`用户确认 > 正式规格书 / 安装手册 > 公司 Agent > 其他方案资料`。

## 已确认规则

| 主题 | Agent / 资料证据 | 用户最终口径 | 当前状态 |
| --- | --- | --- | --- |
| 大圆盘阵麦处理器 | 设备接口资料用于核对阵麦输入关系 | 只能连接 AJ350；1 只接 A1，2 只接 A1/A2；不得改用 AJ200/AJ600 | 已实现硬规则 |
| RING08 数量边界 | 资料支持处理器直连阵麦 | 最多 2 只；超过 2 只产生硬风险，不通过更换处理器绕过 | 已实现 |
| SA110 直连 | Agent 接线图和既有资料提供接口参考 | 有空闲阵麦接口时优先直连：AJ350 使用 AMIC，AJ200/AJ600 使用 EXTMIC | 5174/5180 正式规则保持不变，候选接线图已实现 |
| SA110 背面接口图 | Agent 检索称知识库只有正面图，未找到 SA110 背面图 | 用户先确认 SA100 接口图可用于 SA110，随后直接提供 SA110 背面接线图；最终使用用户提供的 SA110 图 | 已导入候选图；RJ45 按真实位置接线并通过页面校验 |
| SA110 转换 | 线阵拓展器实物图只确认 LINK 侧 | 直连接口不足时，每只 SA110 配 1 个线阵拓展器，并占处理器 2 个 MIC 口 | 候选接线图已实现 |
| SA110 + 02 | 既有资料没有形成完整自动分配结论 | 1 只 SA110 + 02 使用 AJ200；SA110 经拓展器接 MIC1/MIC2，02 级联链接 EXTMIC；其他接口不足再升 AJ600 | 已实现并测试 |
| 两只 SA110 | 既有 FAE 资料只覆盖部分组合 | 无 02 时 AJ600：1 只直连 EXTMIC，另 1 只经拓展器接 MIC1/MIC2；带 02 时两只均经拓展器占 MIC1-MIC4，02 共用 EXTMIC | 候选接线图已实现并测试 |
| 两类拓展器 | Agent 曾提供 RingOf-A 经 LINK 连接 RING01 的说明 | 01拓展器与线阵拓展器是两个独立产品；01/03 的 LINK 只连接 01拓展器，A IN/A OUT 按信号方向分配 | 已实现 |
| USB 音频 | Agent / 接线图提供 USB 接口证据 | USB 音频只能连接电脑或一体机；电脑也可改用模拟 A IN/A OUT；03 的 USB/Type-C 只用于调试，不进入客户接线图 | 已实现 |
| 无线接收机背面图 | Agent 从《手持麦接收机说明书》检索到 `img_9b36155b6699`，可看清 BAL OUT、LINE OUT、MIC OUT 和物理位置 | 可作为无线接收机接口图；实际接线仍按方案使用的输出口分配；BAL OUT 从左到右为 `+ / - / G` | 已导入候选图并通过三芯物理端子校验 |
| 小圆盘层级 | Agent 资料只描述连接，不决定绘图层级 | 01、03 自身为一级中心；01拓展器为二级；二级围绕一级均匀分布，三级围绕所属二级 | 已实现 |

## 冲突记录

| Agent / 旧资料说法 | 用户确认口径 | 最终采用 |
| --- | --- | --- |
| Agent 资料曾出现小圆盘可桌面摆放的描述 | 01、02、03 都必须吊杆安装，不能桌面摆放 | 采用吊杆安装 |
| 早期 FAE 结论倾向于 SA110 后场不足时改用 RING08 | 允许前场 SA110 + 后场 02，并按接口需求自动使用 AJ200/AJ600 | 采用混合拾音规则 |
| 旧代码和旧测试允许大圆盘阵麦读取双麦/六麦处理器手动档位 | 大圆盘阵麦只能接 AJ350 | 忽略历史档位，强制 AJ350 |
| Agent 回答知识库没有 SA110 背面接口图，并建议仅参考 SA100 | 用户明确 SA100 可用于 SA110，随后提供了 SA110 实际背面接线图 | 直接采用用户提供的 SA110 图，不再使用替代图 |

## 待用户手工补充

以下字段不得根据其他产品类推；补齐前在内部接线图中显示“需复核”，不阻断其他已确认连线。

### 线阵拓展器输出侧

- 输出 1 面板原始标识：`[待用户填写]`
- 输出 1 物理接口形式：`[待用户填写]`
- 输出 2 面板原始标识：`[待用户填写]`
- 输出 2 物理接口形式：`[待用户填写]`
- 两路输出是否有固定 1/2 顺序或极性要求：`[待用户填写]`

### 未知外接设备

- 录播主机具体音频输入 / 输出接口形式：按项目设备型号专项复核。
- 会议平台终端具体音频输入 / 输出接口形式：按项目设备型号专项复核。
- 摄像机、中控主机是否具备可用音频接口：未提供接口图时不得自动连线。

## Agent 询问格式

后续每次只问一个具体问题，优先让 Agent 回答“是 / 不是”。示例：`线阵拓展器输出1的面板原始标识是否为 MIC OUT 1？` 若回答“不是”或“不完全”，再追问实际标识与适用边界。Agent 与本文件用户确认口径冲突时，继续以用户口径为准并追加冲突记录。
